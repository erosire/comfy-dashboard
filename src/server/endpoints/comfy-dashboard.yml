openapi: 3.0.3
info:
  title: ComfyUI Dashboard API
  description: >-
    API for managing ComfyUI workflows and cloud pod management. Provides
    CRUD for workflows and a unified endpoint for spawning/checking Beam
    cloud pods.
  version: 0.2.0
servers:
  - url: http://127.0.0.1:8188
    description: Local ComfyUI server
  - url: https://comfy-spawner.beam.cloud
    description: Beam ComfyUI spawner (Tier 1 — used by POST /v1/comfy/cloud)
tags:
  - name: Workflows
    description: CRUD for ComfyUI workflows
  - name: Cloud
    description: Beam cloud pod management — spawn pods and check pod status
paths:
  # ── Workflows ──────────────────────────────────────────────────────

  /v1/comfy/workflows:
    get:
      operationId: listWorkflows
      summary: List all workflows
      description: >-
        Returns stored workflow metadata, optionally filtered by search query
        (matches name, description, or tags). Each entry includes id, name,
        description, nodeCount, timestamps, and tags. Sorted by modifiedDate
        descending (most recently modified first).
      tags:
        - Workflows
      parameters:
        - name: q
          in: query
          required: false
          description: >-
            Free-text search query. Matches against workflow name, description,
            and tags (case-insensitive substring match).
          schema:
            type: string
      responses:
        '200':
          description: List of all workflows
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowListResponse'
              example:
                workflows:
                  - id: wf-001
                    name: Text-to-Image Basic
                    description: Simple txt2img workflow
                    nodeCount: 8
                    createdDate: '2026-07-01T10:00:00.000Z'
                    modifiedDate: '2026-07-01T10:00:00.000Z'
                    tags: [txt2img, basic]
    post:
      operationId: createWorkflow
      summary: Create a new workflow
      description: >-
        Uploads a new ComfyUI workflow. Accepts the workflow name, optional
        description, and the raw ComfyUI workflow JSON. Extracts node
        information and stores the workflow.
      tags:
        - Workflows
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkflowRequest'
      responses:
        '200':
          description: Workflow created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateWorkflowResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /v1/comfy/workflows/{id}:
    get:
      operationId: getWorkflow
      summary: Get a specific workflow
      description: >-
        Returns the full workflow including all nodes and the raw ComfyUI JSON.
      tags:
        - Workflows
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Workflow details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetWorkflowResponse'
        '404':
          description: Workflow not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: updateWorkflow
      summary: Update a workflow
      description: >-
        Partially updates an existing workflow. Accepts any combination of
        name, description, and raw workflow JSON. Only provided fields are
        updated; omitted fields retain their current values.
      tags:
        - Workflows
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkflowRequest'
      responses:
        '200':
          description: Workflow updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateWorkflowResponse'
        '404':
          description: Workflow not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: deleteWorkflow
      summary: Delete a workflow
      description: Permanently removes a workflow and all its data.
      tags:
        - Workflows
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Workflow deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteWorkflowResponse'
        '404':
          description: Workflow not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ── Cloud ──────────────────────────────────────────────────────────

  /v1/comfy/cloud:
    post:
      operationId: cloudPod
      summary: Create a cloud pod or check pod status
      description: >-
        Dual-purpose endpoint for Beam cloud pod management:

        **Create a pod** — Send an empty body `{}` or `{"name": "my-pod"}`.
        Spawns a fresh, isolated ComfyUI pod via the Beam spawner (Tier 1)
        and returns the pod's container ID and public proxy URL. The pod
        runs ComfyUI with a FastAPI proxy on its public port and scales to
        zero ~120s after the last active connection.

        **Check pod status** — Send `{"pod_url": "<pod_url>"}`. Probes the
        Tier 2 proxy of an existing pod and returns its health, system stats,
        and available models. Use this to verify a pod is ready before
        submitting prompts.
      tags:
        - Cloud
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloudPodRequest'
            examples:
              create_pod:
                summary: Spawn a new pod
                value: {}
              create_named_pod:
                summary: Spawn a pod with a custom name
                value:
                  name: my-comfy-pod
              check_status:
                summary: Check the status of an existing pod
                value:
                  pod_url: "https://test-pod-abc123.beam.cloud:8188"
      responses:
        '200':
          description: >-
            Pod created or status returned successfully.
            The response shape depends on the request:
            - **Create**: `{ container_id, pod_url }`
            - **Status**: `{ health, models_dir, models }`
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/CloudCreateResponse'
                  - $ref: '#/components/schemas/CloudPodStatusResponse'
              examples:
                create_result:
                  summary: Pod created successfully
                  value:
                    container_id: "5f8c1b2a-3d4e-4f5a-9b8c-7e6d5c4b3a2d"
                    pod_url: "https://test-pod-abc123.beam.cloud:8188"
                status_result:
                  summary: Pod status returned
                  value:
                    health:
                      healthy: true
                      system_stats:
                        system:
                          os: linux
                          ram_total: 25728937984
                          ram_free: 20000000000
                          comfyui_version: "0.3.27"
                          python_version: "3.10.12"
                          pytorch_version: "2.5.1+cu130"
                        devices:
                          - name: "NVIDIA GeForce RTX 4090"
                            type: cuda
                            index: 0
                            vram_total: 25769803776
                            vram_free: 25600000000
                    models_dir: /ComfyUI/models
                    models:
                      checkpoints:
                        - sd_xl_base_1.0.safetensors
                      loras:
                        - wan/lightning_4steps.safetensors
                      vae:
                        - sdxl_vae.safetensors
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Spawner unavailable, pod creation failed, or pod unreachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

# =============================================================================
#  COMPONENTS
# =============================================================================
components:
  schemas:

    # ── Workflows ──────────────────────────────────────────────────

    WorkflowMeta:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        nodeCount:
          type: integer
        createdDate:
          type: string
        modifiedDate:
          type: string
        tags:
          type: array
          items:
            type: string

    Workflow:
      allOf:
        - $ref: '#/components/schemas/WorkflowMeta'
        - type: object
          properties:
            nodes:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                  type:
                    type: string
                  position:
                    type: object
                    properties:
                      x:
                        type: number
                      y:
                        type: number
                  inputs:
                    type: object
            raw:
              type: object
              description: The raw ComfyUI workflow JSON

    CreateWorkflowRequest:
      type: object
      required:
        - name
        - raw
      properties:
        name:
          type: string
        description:
          type: string
        raw:
          type: object
          description: The raw ComfyUI workflow JSON

    CreateWorkflowResponse:
      type: object
      properties:
        workflow:
          $ref: '#/components/schemas/WorkflowMeta'

    GetWorkflowResponse:
      type: object
      properties:
        workflow:
          $ref: '#/components/schemas/Workflow'

    DeleteWorkflowResponse:
      type: object
      properties:
        success:
          type: boolean
        id:
          type: string

    UpdateWorkflowRequest:
      type: object
      description: >-
        Partial update for an existing workflow. Only include fields that
        should change; omitted fields retain their current values.
      properties:
        name:
          type: string
          description: New name for the workflow.
        description:
          type: string
          description: New description for the workflow.
        raw:
          type: object
          description: >-
            Updated raw ComfyUI workflow JSON. When provided, the node list
            and nodeCount are re-extracted from this object.
        tags:
          type: array
          items:
            type: string
          description: Replacement tag list.

    UpdateWorkflowResponse:
      type: object
      properties:
        workflow:
          $ref: '#/components/schemas/Workflow'

    WorkflowListResponse:
      type: object
      properties:
        workflows:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowMeta'

    # ── Cloud ──────────────────────────────────────────────────────

    ErrorResponse:
      type: object
      properties:
        error:
          type: string

    CloudPodRequest:
      type: object
      description: >-
        Request body for POST /v1/comfy/cloud.
        - Empty `{}` or `{"name": "..."}` → spawns a new pod.
        - `{"pod_url": "<url>"}` → checks status of an existing pod.
      properties:
        name:
          type: string
          description: Optional name for the spawned pod (create mode only).
        pod_url:
          type: string
          format: uri
          description: >-
            Tier 2 proxy URL of an existing pod. When provided, the endpoint
            returns the pod's health and models listing instead of creating
            a new pod.
          example: https://test-pod-abc123.beam.cloud:8188

    CloudCreateResponse:
      type: object
      description: Response from spawning a new ComfyUI pod via Beam.
      properties:
        container_id:
          type: string
          format: uuid
          description: Beam container ID of the spawned pod.
          example: 5f8c1b2a-3d4e-4f5a-9b8c-7e6d5c4b3a2d
        pod_url:
          type: string
          format: uri
          description: >-
            Public URL of the spawned pod's Tier 2 API proxy.
            Use this URL for submitting prompts directly to the pod.
          example: https://test-pod-abc123.beam.cloud:8188
      required: [container_id, pod_url]

    CloudPodStatusResponse:
      type: object
      description: >-
        Health and model listing for a running ComfyUI pod.
        Returned when POST /v1/comfy/cloud is called with a pod_url.
        Mirrors the Tier 2 proxy GET / response from the Beam ComfyUI service.
      properties:
        health:
          $ref: '#/components/schemas/ProxyHealthResponse'
        models_dir:
          type: string
          example: /ComfyUI/models
          description: Absolute path to the (symlinked) models directory.
        models:
          $ref: '#/components/schemas/ModelsListing'
      required: [health, models_dir, models]

    ProxyHealthResponse:
      type: object
      description: Result of probing the backgrounded ComfyUI's GET /system_stats.
      properties:
        healthy:
          type: boolean
          example: true
        system_stats:
          $ref: '#/components/schemas/ComfySystemStats'
          description: Present only when healthy is true.
        error:
          type: string
          description: Present only when healthy is false. Last error message.

    ComfySystemStats:
      type: object
      description: Subset of ComfyUI's GET /system_stats body (forwarded verbatim).
      properties:
        system:
          type: object
          properties:
            os:
              type: string
              example: linux
            ram_total:
              type: integer
              format: int64
              example: 25728937984
            ram_free:
              type: integer
              format: int64
              example: 20000000000
            comfyui_version:
              type: string
              example: "0.3.27"
            python_version:
              type: string
              example: "3.10.12"
            pytorch_version:
              type: string
              example: "2.5.1+cu130"
        devices:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: "NVIDIA GeForce RTX 4090"
              type:
                type: string
                example: cuda
              index:
                type: integer
                example: 0
              vram_total:
                type: integer
                format: int64
                example: 25769803776
              vram_free:
                type: integer
                format: int64
                example: 25600000000
              torch_vram_total:
                type: integer
                format: int64
              torch_vram_free:
                type: integer
                format: int64

    ModelsListing:
      type: object
      description: >-
        Recursive listing of /ComfyUI/models (the symlinked HuggingFace
        models volume). Each KEY is a top-level model subfolder name
        (e.g. checkpoints, loras, vae, clip, controlnet).
        Each VALUE is the sorted list of file names found anywhere under that
        subfolder (relative to the subfolder root). May be empty if no models
        have been uploaded.
      additionalProperties:
        type: array
        items:
          type: string
      example:
        checkpoints:
          - sd_xl_base_1.0.safetensors
          - sd_xl_refiner_1.0.safetensors
        loras:
          - wan/lightning_4steps.safetensors
        vae:
          - sdxl_vae.safetensors
